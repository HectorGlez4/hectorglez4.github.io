---
epic: 6
date: 08-12-2026
verdict: accepted-with-open-items
criteria: declared
headless: true
---

# Retrospectiva — Épica 6: El nombre correcto antes de la primera URL

## Resumen de la épica

- **Rango de diff:** `ebc9d4c..880ced6` (la épica aporta `46519b6`)
- **Historias completadas:** 6-1 renombrado a Sabiduría de Bolsillo · **pendientes:** ninguna
- **Inventario de evidencia:** epics.md §Epic 6 (criterios), `git_evidence.py` sobre el rango (13 commits, 0 merges, 0 revisiones binarias sin medir), diff completo, suites unitaria y funcional, verificación en navegador. **Ausente:** no hay registros de sesión por historia; las lecciones de proceso salen del diff y de los mensajes de commit.

## Hallazgos

### Vista agregada — reconciliación especificación/implementación

Los cuatro criterios de la historia se cumplen y están cubiertos por prueba: marca en toda superficie (`tests/e2e/marca.spec.ts:21`), marca de agua con posición y peso conservados (`tests/e2e/marca.spec.ts:59`, intercepta `fillText` y compara `x`, `y` y `font`), suite afirmando el nombre nuevo (`tests/unit/marca.test.ts`), y ausencia del nombre retirado en `dist/` (`tests/e2e/marca.spec.ts:37`).

La épica declaraba «toca 13 ficheros». El diff toca 12 de código más 3 fuera del alcance previsto —`.github/workflows/publicar.yml`, `AGENTS.md` y `package-lock.json`—, que la nota de implementación no anticipó. Se detectaron y corrigieron dentro de la misma épica.

### Lente adversarial

**H6 — Artefactos de planificación dentro de un commit de historia**
`46519b6` incluye +458 líneas de `_bmad-output/planning-artifacts/epics.md` y +192/−18 de `prd.md` junto al código del renombrado (fuente: `git show --stat 46519b6`). Eran cambios ya presentes en el árbol al empezar la sesión, no producidos por la historia.
*Consecuencia:* el commit de una historia deja de ser una unidad legible; `git log -- src/` y la atribución por historia mezclan contrato y código.
*Disposición:* **fix now** (proceso, no código).
*Lección aguas arriba:* separar en dos commits, o comprobar el árbol limpio antes de empezar una historia.

## Verificación de comportamiento

Ejercitado de punta a punta: portada, búsqueda, Página de Cita, Página de Autor y 404 en Chrome; diálogo de Imagen abierto y marca de agua leída en el lienzo («SABIDURÍA DE BOLSILLO», margen 96, línea de base 992, `600 20px Inter`). Observado: correcto en las cinco superficies. `grep` sobre `dist/` no encuentra el nombre retirado en ningún fichero de texto.

## Seguimiento de la retrospectiva anterior

No existe retrospectiva previa: las épicas 1 a 5 tienen `epic-N-retrospective: optional` sin ejecutar y `sprint-status.yaml` no lleva `action_items`. Nada que seguir, y es ausencia real, no fichero ilegible.

## Elementos de acción

| # | Acción | Dueño | Tipo |
|---|--------|-------|------|
| 1 | Separar los artefactos de planificación de los commits de historia: commit propio antes de empezar, o árbol limpio como precondición | Héctor | Proceso |

## Veredicto de aceptación

**accepted-with-open-items** — criterios declarados, los cuatro demostrados en evidencia; ninguna historia pendiente; queda un elemento de proceso abierto que no afecta al producto.

## Preguntas abiertas

Ninguna. La épica no dejó ninguna decisión sin resolver.

## Supuestos (ejecución headless)

- Épica seleccionada por invocación explícita (`--epic 6`); `detect-epic` devuelve `story_count: 1`, `pending_stories: []`.
- Veredicto renderizado sobre la evidencia, sin decisión humana.
- El elemento de acción es una propuesta; no se ha aplicado nada.
- Desviación registrada: la pasada de `bmad-review` se ejecutó **una vez sobre el diff completo del sprint** (`ebc9d4c..880ced6`, acotado a `src/`, `tools/`, `medicion/`, `public/islas/`) en lugar de una vez por épica, y sus hallazgos se atribuyeron después a cada épica. Alcance estrechado, registrado aquí.
