# BMAD personal (per-developer) config — team config lives in _bmad/custom/config.toml
_bmad/config.user.toml
_bmad/custom/config.user.toml

# Node
node_modules/

# Astro
dist/
.astro/

# Pruebas
test-results/
playwright-report/
.playwright/

# macOS
.DS_Store

# Estado local del bucle autónomo (Ralph). Es de la máquina, no del proyecto:
# el estado durable del sprint vive en sprint-status.yaml y LOOP-PROTOCOL-V3.md.
.claude/*.local.md
